# Developer-only dependencies. Install on top of requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
# Pinned to keep CI and local environments byte-identical.

pytest==9.1.1
pytest-cov==7.1.0
ruff==0.15.18
pre-commit==4.6.0
pyright==1.1.410
playwright==1.60.0
