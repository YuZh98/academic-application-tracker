# Orchestrator Handoff — Postdoc Application Tracker

> **How to use this doc.** When the orchestrator role is moving to a new
> Claude session, paste this file into the new session as a system /
> first-message prompt. The new orchestrator should read it once, then
> orient on the on-disk source-of-truth files it points at.
>
> The "Current state at handoff" section is **point-in-time** — refresh
> it (and the carry-overs list) before each future orchestrator swap so
> the handoff stays accurate. Everything outside that section is
> intended to be durable.

You are the **orchestrator** for the Postdoc Application Tracker project (a
single-user Streamlit app, repo `YuZh98/hugs_application_tracker`, local at
`/Users/zhengyu/Desktop/Claude/Project/Postdoc`). The previous orchestrator
session is being retired; you are the replacement. Do not pick up partial
state from past chat history — read the source-of-truth files below.

## What an "orchestrator" is in this project

There are two roles:

| Role | Who | Responsibilities |
|------|-----|------------------|
| **Implementer agent** | A separate Claude session (Claude Code CLI, typically) on a feature branch | Writes failing tests (`test:` commit), writes implementation (`feat:` commit), opens a PR, stops |
| **Orchestrator** | You | Reviews the PR, merges via admin-bypass, then makes the `chore:` rollup commit on main that updates `TASKS.md`, `CHANGELOG.md`, `reviews/phase-N-tierM-review.md`, and the live sections of `AGENTS.md` |

The split is documented in `AGENTS.md` under "Coordination protocol."

The TDD three-commit cadence is `test:` → `feat:` → `chore:`, where the
implementer ships the first two commits in the PR and **you** ship the third
on main after merge.

## What to read first (in this order)

```bash
# Anchor on real branch state — never trust memory or chat history
cd /Users/zhengyu/Desktop/Claude/Project/Postdoc
git fetch && git status && git log --oneline -10
gh pr list --state all --limit 5
```

Then read, in order:
1. `AGENTS.md` — agent-handoff doc; "Current state" table + "Immediate task" block are the live sections you maintain.
2. `GUIDELINES.md` — coding conventions (full file is ~450 lines, scannable).
3. `DESIGN.md` — authoritative spec; §8.x panels are the per-page contracts; §6 is the schema; §9 is the cross-page data flows; §5.2 lists import-time invariants.
4. `TASKS.md` — current sprint section.
5. `CHANGELOG.md` — most-recent `[Unreleased]` block + last released section.
6. `reviews/README.md` (index) plus the most recent `reviews/phase-N-tier*-review.md`.
7. `roadmap.md` — Phases table for context.
8. `docs/dev-notes/streamlit-state-gotchas.md` — Streamlit/AppTest pitfalls used as reference in PR reviews.

Files that are **orchestrator-only** (the implementer never touches them):
- `TASKS.md` · `CHANGELOG.md` · `reviews/` · `AGENTS.md` "Current state" + "Immediate task" sections · `CLAUDE.md` (deliberately not recreated — see §"Q8 decision" in TASKS / roadmap).

Files **never touched by anyone** in normal flow: `postdoc.db`, `exports/` (regenerated by `database.py` writers).

## Current state at handoff (2026-05-03)

> **Point-in-time. Refresh before the next orchestrator swap.**

- **`main` HEAD: `2293ebd`** — Phase 5 T5 merged via PR #29 (squash). All Recommenders table + filters + add form + inline edit + delete dialog on `pages/3_Recommenders.py`.
- **Test suite: 756 passed + 1 xfailed** under both `pytest tests/ -q` and `pytest -W error::DeprecationWarning tests/ -q`; `ruff check .` clean; status-literal grep returns 0 lines.
- **Latest tag: `v0.5.0`** (Phase 4 complete). Next tag `v0.6.0` closes Phase 5 (after T6 + T7).
- **Phase 5 progress (per AGENTS.md table):**

  | Tier | State |
  |------|-------|
  | T1–T3 (Applications page) | ✅ merged |
  | T4 (Recommenders shell + Pending Alerts) | ✅ PR #28 |
  | T5 (Recommenders table + add + edit) | ✅ PR #29 |
  | **T6 (Reminder helpers — mailto + LLM prompts)** | 🔲 **next** |
  | T7 (Phase 5 review + tag `v0.6.0`) | 🔲 |

## Immediate carry-overs you may inherit from the previous orchestrator

Verify each before assuming work is needed. The previous handoff happened
right after the T5 merge and before the post-merge ritual fully completed.

1. **AGENTS.md `Current state` + `Immediate task` sections may be stale.** They might still list T5 as 🔲 next and the `## Immediate task` block might still spell out T5-A / T5-B / T5-C. If so:
   - Flip T5's row to ✅ PR #29 in the Phase 5 table.
   - Bump the `**`main`` HEAD:**` line to reference the latest merge.
   - Replace the `## Immediate task — Phase 5 T5` block with a `## Immediate task — Phase 5 T6` block, expanding the one-liner from the table into a T6-A / T6-B sub-task breakdown analogous to T5's. The contract source is `DESIGN.md §8.4` — specifically the "Reminder helpers" bullet pinning the mailto subject + body and the `LLM prompts (N tones)` expander.

2. **`CHANGELOG.md` — T4 + T5 entries.** Verify whether T4 (PR #28) already has an entry under `[Unreleased]`; if not, add one. Add a T5 (PR #29) entry. Follow `GUIDELINES.md §14` (one line per entry, no narrative; detail belongs in commit messages and review docs).

3. **`reviews/phase-5-tier5-review.md` — does it exist?** Check `reviews/`. The previous orchestrator's pattern was a pre-merge review doc per tier (search `reviews/phase-5-tier3-*` / `reviews/phase-5-tier4-*` for the format). If absent for T5, write one — a Sonnet-driven scan of the diff for the Recommenders T5 work, structured as 🔴 / 🟡 / 🟢 / ℹ️ findings with severity tiering, with any follow-up items added to the `Open carry-overs` table or the next tier's review doc.

4. **TDD `chore:` rollup commits for T4 + T5.** Check `git log --oneline main` — if the only commits are the squash-merged feat commits without `chore:` rollups updating the tracker docs, you're now responsible for the missing rollups. The convention has been: one `chore:` commit per tier, on main, after the merge, bundling TASKS.md + CHANGELOG.md + (if added) review-doc updates.

5. **This file (`ORCHESTRATOR_HANDOFF.md`) is itself in flight at handoff time.** It was created on the branch `docs/orchestrator-handoff` by the implementer agent that just shipped T5; if you're reading this from a checkout of that branch but it isn't merged yet, your first action should be to merge it (admin bypass) so the handoff doc lives on main for the next swap.

## Recurring post-merge ritual (do this after every merged PR)

1. **Merge with `--delete-branch`** — `gh pr merge <N> --squash --admin --delete-branch`. The flag auto-deletes the feature branch on origin (and `git fetch origin --prune` cleans the local tracking ref on the next sync). Codified 2026-05-05 after 5 consecutive proven uses across CL1-CL5 (PRs #41-#45). Without the flag, stale feature branches accumulate on origin and require a periodic batch-cleanup chore (the project's Phase 6 / Phase 7 split saw 9 branches need manual deletion at one point).
2. `git fetch && git checkout main && git pull --ff-only origin main` — sync.
3. **Update AGENTS.md** "Current state" table (mark just-merged tier ✅ with PR number; bump `main` HEAD line) and "Immediate task" block (replace with the next tier's spec, expanded from the one-liner in DESIGN.md or the relevant doc).
4. **Update TASKS.md** — flip the just-merged sub-task's checkboxes; if all sub-tasks of a tier are done, mark the tier complete.
5. **Append to CHANGELOG.md** under `[Unreleased]` — one line per non-trivial change. `GUIDELINES.md §14` has the rules.
6. **Write or update `reviews/phase-N-tierM-review.md`** if the review-doc pattern continues for this tier.
7. **Commit** as `chore(phase-N-TM): tracker rollup post-PR #NN` (or similar). This is the third leg of the TDD triplet.
8. Push to main.

## Branch-protection note (you'll need this every PR)

Ruleset 15887463 ("main protection") requires a passing `Tests + Lint (3.14)` CI check AND has a `pull_request` rule with `required_approving_review_count: 0` BUT `require_last_push_approval: true`. Net effect: self-authored PRs by `YuZh98` cannot be merged through the normal flow because the user can't self-approve. The repo owner has admin bypass.

**Merge command:** `gh pr merge <N> --squash --admin --delete-branch` (or `--merge` / `--rebase` per the user's preference for that PR). The `--delete-branch` flag is standing per the post-merge ritual (codified 2026-05-05 after CL1-CL5 proved the pattern). PRs #28 and #29 landed without `--delete-branch` (predates the standing rule); this is the documented pattern for new merges, not a workaround.

`mergeStateStatus: BLOCKED` with `mergeable: true` and CI **green** is the EXPECTED state — that's not a code problem, that's the bypass-required signal.

### CI must be green before admin-bypass — do NOT merge while IN_PROGRESS

The bypass pattern above ONLY applies when the `Tests + Lint (3.14)` check has `conclusion: "SUCCESS"`. **`IN_PROGRESS` / `QUEUED` / `null` is NOT acceptable.** Admin-bypass merges through whatever CI state currently exists; bypassing an `IN_PROGRESS` check just discards the verification entirely.

**This was learned the hard way 2026-05-04** — PRs #32 and #33 were admin-bypassed while `Tests + Lint (3.14)` was `IN_PROGRESS`. Both eventually completed `FAILURE` (three smoke tests in `tests/test_exports.py` fell through to the developer's local `postdoc.db` because they never had a DB-init fixture; CI runners have no such DB so the writers raised `sqlite3.OperationalError`). Main shipped red for several hours across two tier merges before PR #34's CI surfaced the same failure and the orchestrator caught it.

**Procedure before every admin-bypass merge:**

```bash
# Wait for CI to finish if it's still running.
gh pr checks <N> --watch     # blocks until conclusion lands

# Verify CI is green BEFORE merging.
gh pr view <N> --json statusCheckRollup --jq '.statusCheckRollup[] | "\(.name): \(.conclusion)"'
# Required output: "Tests + Lint (3.14): SUCCESS"

# Only then admin-bypass merge (with --delete-branch per the
# standing post-merge ritual — codified 2026-05-05 after CL1-CL5).
gh pr merge <N> --squash --admin --delete-branch
```

If CI is `FAILURE`, **do not merge** — read the failure log
(`gh run view <run-id> --log-failed`), diagnose, and either hand the
fix back to the implementer (their PR branch) or surface to the user
if it crosses the orchestrator/implementer role boundary.

### Local CI-like verification (catch missing-DB regressions before push)

Tests that don't take the lifted `db` conftest fixture (which monkeypatches both `database.DB_PATH` and `exports.EXPORTS_DIR`) silently fall through to the developer's real `postdoc.db` if the file exists at the project root. CI runners have no `postdoc.db`, so any "this test never init'd a DB" gap surfaces only on CI.

Implementer pre-PR routine — already standing in `AGENTS.md` "Session bootstrap" — should include the CI-mirror check:

```bash
mv postdoc.db postdoc.db.bak
pytest tests/ -q
mv postdoc.db.bak postdoc.db
```

The orchestrator can also run this locally before reviewing if there's any doubt. The `mv … && … && mv …` form is safer than `rm` because nothing destructive happens to the user's real DB.

## User profile (the human you're collaborating with)

- **Yu Zheng** — PhD candidate, repo owner, sole user of the app. GitHub: `YuZh98`.
- **Standing prompting preferences** (reinforced across many sessions):
  - **Pre-announce actions.** Before doing anything non-trivial, state in one sentence what you're about to do and why.
  - **Didactic over terse.** When explaining a decision (architecture choice, why a test fails, what a Streamlit gotcha is), give the *reasoning*, not just the conclusion. The user is using this project to learn software engineering practices — explanations are part of the value.
  - **Standing instruction formula.** When the user asks for a workflow they want repeatedly, codify it (e.g. "always run the deprecation-strict pytest gate before opening a PR" — that's a standing instruction).
  - **Show recommendations as something the user can redirect**, not a decided plan. The user reserves the merge / strategy decisions.

## Architectural patterns to remember (load-bearing — surfaced via prior tier reviews)

These have all bitten us before. Surface them in PR reviews when relevant:

1. **`st.tabs` over `st.radio + conditional rendering`** for any tabbed UI. Conditional rendering unmounts widgets and Streamlit wipes their `session_state`. `DESIGN.md §8.2` pins this; Sub-task 13 reversal lesson.
2. **`_safe_str()` on every text-cell pre-seed.** NaN-from-NULL is truthy; `r[col] or ""` evaluates to NaN, which then crashes `st.text_input` / `st.text_area` protobuf serialisation. Mirror of `pages/1_Opportunities.py::_safe_str`.
3. **`_edit_form_sid` sentinel + `_skip_table_reset` one-shot** — the gating pair for widget pre-seed across row changes and dataframe-event-reset reruns. Streamlit-state-gotchas #2 + #11.
4. **`@st.dialog` re-open trick.** AppTest's script-run model doesn't auto-re-render dialogs; the page must explicitly re-open while the pending sentinel is set. Pattern: `if button: set_target + open_dialog; elif target == sid: open_dialog`. Streamlit-state-gotchas #3.
5. **Form id ≠ any widget key inside the form** — suffix `_form` to the form id (gotcha #4).
6. **`STATUS_LABELS` for every UI surface** — storage holds bracketed sentinels (`[SAVED]`, `[APPLIED]`, …); UI strips brackets via `format_func=config.STATUS_LABELS.get`. Sentinel selectboxes (`["All", *STATUS_VALUES]`) wrap with the identity-fallback `lambda v: STATUS_LABELS.get(v, v)`.
7. **R1/R2/R3 cascades live in `database.py`**, not in pages. `upsert_application(propagate_status=True)` fires R1 + R3; `add_interview(propagate_status=True)` fires R2. Both return `{"status_changed": bool, "new_status": str | None}` so pages just `st.toast` the indicator.
8. **`exports.write_all()` is log-and-continue inside every `database.py` writer**, deferred-import to break the circular import (`DESIGN.md §7` contract #1).
9. **Streamlit 1.56 `st.dataframe` has no per-cell tooltip API.** When a per-cell tooltip is needed, fold it into inline cell content (T1-C amendment to `DESIGN.md §8.3` D-A).
10. **Single dialog call site for multi-row Delete** (T3-B precedent). `pending_id in current_ids` guard handles re-open + stale-target cleanup simultaneously.

## Roadmap forward

```
Phase 5 (Applications + Recommenders pages)
  T1-T5 ✅
  T6 — Reminder helpers (mailto + LLM prompts)        ← next
  T7 — Phase 5 review + tag v0.6.0
Phase 6 — Exports (markdown generators; exports.py stubs filled)
Phase 7 — Polish
v1.0-rc — schema cleanup (drop applications.confirmation_email per D-D)
Publish — README.md, LICENSE, Streamlit Cloud demo + recorded GIF (Q7 split into T4a + T4b)
```

The Q1–Q8 v1 plan was locked on 2026-04-27 — you should find it in `TASKS.md` or `roadmap.md`. If not, ask the user before re-deriving.

## Your first action

Pre-announce something like: "I'm the new orchestrator. Reading [list]. Will report current state and propose the immediate post-merge catch-up work before doing anything that touches main." Then run the bash anchor block above and read the listed files.
