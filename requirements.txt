# Generated: 2026-04-15 | python3 -m pip freeze | Python 3.14.0 | macOS arm64
# Compatible with Python >=3.11 (declared in pyproject.toml [project]).
# To regenerate: source .venv/bin/activate && pip freeze > requirements.txt
#
# Direct deps (everything else is a transitive dep required by one of these
# or their dependents — verified 2026-05-05 via `pip show` reverse-dep
# audit; zero orphans found):
#   streamlit==1.57.0  — UI framework (the entire app surface)
#   pandas==3.0.2      — data tables (st.dataframe + DB→table reads)
#   plotly==6.7.0      — Application Funnel chart on the dashboard
# Dev-only direct deps live in requirements-dev.txt (pytest, ruff,
# pre-commit, pyright).
altair==6.1.0
attrs==26.1.0
blinker==1.9.0
cachetools==7.1.1
certifi==2026.4.22
charset-normalizer==3.4.7
click==8.3.3
gitdb==4.0.12
GitPython==3.1.49
idna==3.13
Jinja2==3.1.6
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
MarkupSafe==3.0.3
narwhals==2.20.0
numpy==2.4.4
packaging==26.2
pandas==3.0.2
pillow==12.2.0
plotly==6.7.0
protobuf==7.34.1
pyarrow==24.0.0
pydeck==0.9.2
python-dateutil==2.9.0.post0
referencing==0.37.0
requests==2.33.1
rpds-py==0.30.0
six==1.17.0
smmap==5.0.3
streamlit==1.57.0
tenacity==9.1.4
toml==0.10.2
tornado==6.5.5
typing_extensions==4.15.0
urllib3==2.6.3
